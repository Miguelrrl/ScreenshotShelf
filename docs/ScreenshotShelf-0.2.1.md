# ScreenshotShelf 0.2.1

- Prevent the native Apple screenshot thumbnail from returning when the app
  quits, restarts, or updates.
- Refresh the cached Screenshot.app preference when ScreenshotShelf starts.
- Keep only the persistent ScreenshotShelf thumbnails visible and stacked.
