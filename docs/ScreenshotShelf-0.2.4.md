# ScreenshotShelf 0.2.4

- Store new screenshots under `Pictures/ScreenshotShelf` instead of Desktop.
- Name saved screenshots with a branded timestamp such as
  `ScreenshotShelf 2026-07-27 at 14.35.22.png`.
- Add the Finder tag `ScreenshotShelf` to saved files.
- Correct recovery of pending files whose names begin with a UUID.
- Document that Preview requires enabling the native Markup toolbar for drawing
  and cropping.
